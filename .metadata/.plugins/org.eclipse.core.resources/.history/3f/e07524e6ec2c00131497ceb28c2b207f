import processing.core.PApplet;

public class Key {
	PApplet parent;
	float ampStart, ampEnd, ampNow;
	float attack, sustain;
	boolean isNoteOn;
	int index;
	float x, y, h, low, hi, w;
	
	public Key(PApplet _parent, int _index) {
        parent = _parent;
        index = _index;
        int ii = index+1;
        float res = parent.width/14.0f;
        x = index*res;
        y = 0;
        h = 50;
        low = 50.0f;
        hi = 100.0f;
    }
	
	public void update(float attack) {
		if (isNoteOn) {
			h = PApplet.map(attack, 0.0f, 1.0f, low, hi);
		} else {
			h = Math.round(b);
			if(h>low) { h--; }
		}
		parent.fill(220, 50, 47);
        parent.rect(x, y, b, b);
	}
}